pipeline {
    agent node1

    stages {
        stage('Checkout') {
            when { changeRequest() }
            steps {
                echo 'Steps'
                sh 'printenv'
            }
        }
    }
}
